export interface SpecificationsEdgeType {
  id_edge: string;
  source_edge: string;
  target_edge: string;
}

export interface postEdgeType {
  id_edge: string;
  source_edge: string;
  target_edge: string;
}
